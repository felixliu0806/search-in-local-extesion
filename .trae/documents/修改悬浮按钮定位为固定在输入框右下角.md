1. 修改 `showButton` 函数，移除基于光标位置的定位逻辑
2. 新增 `getElementPosition` 函数，获取输入框元素的实际位置
3. 修改 `positionButton` 函数，将按钮固定在输入框右下角
4. 确保按钮定位不随输入内容变化
5. 保持按钮的显示/隐藏逻辑不变
6. 测试不同类型输入框（input、textarea、contenteditable）的显示效果

